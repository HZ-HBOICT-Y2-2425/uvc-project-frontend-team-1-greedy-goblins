function sum(x, y) {

    let result = x + y;

    if (result> 0) {
        return ("Positive Result = " + result)
    } else {
        return ("Negative Result = " + result)
    }
}
module.exports = sum;
